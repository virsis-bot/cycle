# <Название проекта>

<!-- Держи файл коротким (≤60 строк): он грузится в КАЖДЫЙ запрос. -->

## Что это
<1–2 строки: что делает проект и для кого>

## Стек
- <язык, фреймворк, БД>

## Команды
- Запуск: `<команда>`
- Проверка всего: `./verify.sh` (тесты, линтер, типы — только ошибки)

## Правила
- Перед задачей читай PLAN.md и NOTES.md, после — обновляй их и коммить.
- Ищи через grep/glob, не читай файлы целиком без нужды.
- Не пиши «готово» без зелёного `./verify.sh`.
- Не меняй SPEC.md и тесты без моего согласия.
- Секреты только в .env, никогда в коде и коммитах.
- `.claude/dev-mode` не трогай: замок снимает Stop hook после зелёной проверки.

## Запреты
- <что нельзя трогать: папки, прод-конфиги, API-ключи>

## Для процесса
Большие задачи — через `/dev <задача>`.
